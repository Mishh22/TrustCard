{
  "name": "trustcard-admin-setup",
  "version": "1.0.0",
  "description": "Admin setup scripts for TrustCard",
  "main": "setup-admin.js",
  "scripts": {
    "setup-admin": "node setup-admin.js",
    "install-deps": "npm install firebase-admin"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
